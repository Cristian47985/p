spring.application.name=cqrs
# Base de datos para escritura (PostgreSQL)
# spring.datasource.write.url=jdbc:postgresql://localhost:5432/write_db
# spring.datasource.write.username=root
# spring.datasource.write.password=abc123.
# spring.datasource.write.driver-class-name=org.postgresql.Driver

# Base de datos para lectura (Cassandra)
# spring.data.cassandra.contact-points=localhost
# spring.data.cassandra.port=9042
# spring.data.cassandra.keyspace-name=read_keyspace
# spring.data.cassandra.username=user
# spring.data.cassandra.password=abc123.
# spring.data.cassandra.schema-action=none



# Base de datos para escritura (PostgreSQL)

# Configuración de la base de datos de escritura
spring.datasource.write.url=jdbc:postgresql://localhost:5432/write_db
spring.datasource.write.username=root
spring.datasource.write.password=abc123.
spring.datasource.write.driver-class-name=org.postgresql.Driver

# Configuración de la base de datos de lectura
spring.datasource.read.url=jdbc:postgresql://localhost:5432/read_db
spring.datasource.read.username=root
spring.datasource.read.password=abc123.
spring.datasource.read.driver-class-name=org.postgresql.Driver

spring.jpa.datasource-platform=org.hibernate.dialect.PostgreSQLDialect




# Configuración de la base de datos
# spring.datasource.url=jdbc:postgresql://localhost:5432/write_db
# spring.datasource.username=root
# spring.datasource.password=abc123.
# spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración del dialecto para PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# Configuración de Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
